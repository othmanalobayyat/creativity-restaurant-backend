# Database Scripts

⚠️ These scripts are one-time database migration utilities.

Do NOT run them in production unless you know what they do.

Each script modifies database structure (ALTER TABLE, etc).
